{
  "name": "poe-recombinator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "data:download": "node scripts/download-data.mjs",
    "data:build": "node scripts/build-data.mjs",
    "data:validate": "node scripts/validate-data.mjs",
    "data": "npm run data:download && npm run data:build && npm run data:validate",
    "dev": "vite",
    "build": "npm run data && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
